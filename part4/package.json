{
  "name": "blog-listings",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Carro Cremello",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development node --watch index.js",
    "test": "cross-env NODE_ENV=test node --test",
    "testapi": "cross-env NODE_ENV=test node --test tests/blog_api.js"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "lodash": "^4.17.23",
    "mongoose": "^9.1.5"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
